<?php
    if ($this->reply_type === Confirmation_Controller::ACTION_ACCEPT) {
    	?>You have accepted user: <? dump($this->entity->toArray());?><?
    }   

    if ($this->reply_type === Confirmation_Controller::ACTION_DECLINE) {
    	?>You have declined user: <? dump($this->entity->toArray());?><?
    }
